apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-setup.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
