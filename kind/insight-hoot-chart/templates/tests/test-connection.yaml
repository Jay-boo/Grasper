apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insight-hoot-chart.fullname" . }}-test-connection"
  labels:
    {{- include "insight-hoot-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insight-hoot-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
